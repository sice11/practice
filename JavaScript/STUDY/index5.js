//let state = true;
//while(state){
    //let random = Math.floor(Math.random()*10);
    //if(random === 3) state = false;
    //console.log(random);
//}

//

//for ( let i = 0; i < 5; i++ ){
    //console.log(i);
    //if(i === 3 ) break;
//}

let state = true;
while(state){
    let random = Math.floor(Math.random()*10);
    if(random === 3)break;
    console.log(random);
}